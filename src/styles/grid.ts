import styled from '@emotion/styled'
import { sizes } from '@/styles/sizes'

export const Row = styled.div`
  display: flex;
  width: 100%;
  max-width: 1472px;
  margin: 0 auto;
  padding: 0 ${sizes(4)};
`
